import 'package:flutter/painting.dart';
import 'package:flutter/widgets.dart';

class Raddi {
  static const BorderRadius buttonCornerRadius =
      BorderRadius.all(Radius.circular(40));

  static const BorderRadius textFieldBorderRadius =
      BorderRadius.all(Radius.circular(10));
  static const BorderRadius k12px = BorderRadius.all(Radius.circular(12));
}

class Margins {
  static const EdgeInsets basePadding = EdgeInsets.all(10);
  static const EdgeInsets listViewContent = EdgeInsets.only(bottom: 18.0);
  static const EdgeInsets marginsK18 = EdgeInsets.all(18.0);
  static const PAGE_CONTENT_MARGINS = EdgeInsets.only(left: 20, right: 20);
  static const SEPARATOR_MARGINS =
      EdgeInsets.only(left: 20, right: 20, top: 15, bottom: 15);
}
